package vn.com.poly.util;

public class StringUtil {
    public static String subString(String message, int beginIndex, int endIndex) {
        return message.substring(beginIndex, endIndex);
    }
}
